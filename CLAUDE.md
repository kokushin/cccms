# Claude Code CMS - ブログ管理ガイド

このファイルは、Claude Code on the webを対話型CMSとして使用するためのガイドです。

## Wake Word（起動ワード）

ブログ管理を開始する際は、以下のwake wordを使用してください：

```
@ブログ管理
```

## ブログ記事の管理

### 記事の作成

新しい記事を作成する場合は、以下のコマンドを使用してください：

```
@ブログ管理 新規記事を作成

タイトル: [記事のタイトル]
日付: [YYYY-MM-DD形式]
著者: [著者名]（オプション）
説明: [記事の説明]（オプション）
内容: [記事の本文（Markdown形式）]
```

**例:**
```
@ブログ管理 新規記事を作成

タイトル: Astroでブログをつくろう
日付: 2025-11-03
著者: 山田太郎
説明: Astroを使ったブログ構築の基本を解説します
内容:
## はじめに
Astroは素晴らしい静的サイトジェネレーターです...
```

### 記事の編集

既存の記事を編集する場合は、以下のコマンドを使用してください：

```
@ブログ管理 記事を編集

ファイル名: [記事のファイル名（例: welcome.md）]
変更内容: [具体的な変更内容の説明]
```

**例:**
```
@ブログ管理 記事を編集

ファイル名: welcome.md
変更内容: タイトルを「ブログへようこそ！」に変更し、本文の第2段落を追加してください
```

### 記事の削除

記事を削除する場合は、以下のコマンドを使用してください：

```
@ブログ管理 記事を削除

ファイル名: [削除する記事のファイル名]
```

**例:**
```
@ブログ管理 記事を削除

ファイル名: old-post.md
```

### 記事一覧の確認

現在公開されている記事の一覧を確認する場合：

```
@ブログ管理 記事一覧を表示
```

## Claude Codeの対応

上記のコマンドを受け取ったClaude Codeは、以下のように動作します：

### 新規記事作成時の動作

1. `src/content/blog/`ディレクトリに新しいMarkdownファイルを作成
2. ファイル名は、タイトルから自動生成（スラッグ化）
3. フロントマター（title, date, author, description）を設定
4. 本文を記述
5. 変更をコミットしてプッシュ

### 記事編集時の動作

1. 指定されたファイルを`src/content/blog/`から読み込み
2. 変更内容を反映
3. 変更をコミットしてプッシュ

### 記事削除時の動作

1. 指定されたファイルを`src/content/blog/`から削除
2. 変更をコミットしてプッシュ

## ファイル構造

```
cccms/
├── src/
│   ├── content/
│   │   └── blog/           # ブログ記事（Markdown）
│   │       ├── welcome.md
│   │       ├── getting-started.md
│   │       └── ...
│   ├── layouts/
│   │   ├── BaseLayout.astro
│   │   └── BlogPostLayout.astro
│   └── pages/
│       ├── index.astro     # トップページ
│       └── blog/
│           ├── index.astro # ブログ一覧
│           └── [...slug].astro  # 個別記事
├── CLAUDE.md               # このファイル
└── astro.config.mjs        # Astro設定
```

## フロントマターの形式

各記事のMarkdownファイルには、以下の形式でメタデータを記述してください：

```yaml
---
title: "記事のタイトル"
date: "YYYY-MM-DD"
author: "著者名"  # オプション
description: "記事の説明文"  # オプション
---

ここから記事の本文をMarkdown形式で記述します。
```

## デプロイ

GitHub Pagesへのデプロイは、mainブランチにプッシュすると自動的に実行されます。

## ローカル開発

ローカルで開発サーバーを起動する場合：

```bash
npm install
npm run dev
```

ビルドする場合：

```bash
npm run build
```

## 注意事項

- 記事のファイル名は、英数字とハイフンのみを使用してください（例: `my-first-post.md`）
- 日付は必ず`YYYY-MM-DD`形式で記述してください
- Markdownの記法については、[CommonMarkの仕様](https://commonmark.org/)を参照してください

## サポート

問題が発生した場合や、新しい機能のリクエストがある場合は、Claude Code on the webで直接質問してください。

---

**Happy Blogging with Claude Code! 🚀**
