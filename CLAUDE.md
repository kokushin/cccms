# Claude Code CMS - ブログ管理ガイド

このファイルは、Claude Code on the webを対話型CMSとして使用するためのガイドです。

自然な言葉でブログ記事の作成・編集・削除ができます。

## ブログ記事の管理

### 記事の作成

自然な言葉で新しい記事を作成できます。

**基本的な言い方:**
```
[タイトル]の記事を作成。概要は[概要の説明]
```

**例:**
```
TypeScriptの基礎という記事を作成。概要はTypeScriptの基本的な使い方を解説します
```

**詳細に指定する場合:**
```
Astroでブログを作ろうという記事を作成。
日付は2025-11-03で、著者は@kokushing。
概要はAstroを使ったブログ構築の基本を解説します。
内容は以下の通り：

## はじめに
Astroは素晴らしい静的サイトジェネレーターです...

## Astroの特徴
...
```

### 記事の編集

既存の記事を自然な言葉で編集できます。

**基本的な言い方:**
```
[ファイル名]の記事を編集。変更内容は[変更の説明]
```

**例:**
```
welcomeの記事を編集。変更内容はタイトルを「ブログへようこそ！」に変更して、新しいセクションを追加
```

```
getting-startedの記事を編集。第2段落に最新のAstroの機能について追記してください
```

### 記事の削除

記事を削除する場合も自然な言葉で指示できます。

**基本的な言い方:**
```
[ファイル名]の記事を削除
```

**例:**
```
old-postの記事を削除
```

```
テスト記事のtest-articleを削除してください
```

### 記事一覧の確認

現在公開されている記事を確認したい場合：

```
記事一覧を表示して
```

```
今どんな記事がある？
```

## Claude Codeの動作

Claude Codeは、自然言語での指示を理解し、以下のように動作します：

### 新規記事作成時の動作

1. `src/content/blog/`ディレクトリに新しいMarkdownファイルを作成
2. ファイル名は、タイトルから自動生成（スラッグ化）
3. フロントマター（title, date, author, description）を設定
4. 本文を記述
5. 変更をフィーチャーブランチにコミットしてプッシュ
6. **mainブランチにマージしてプッシュ**（GitHub Pagesに反映）

### 記事編集時の動作

1. 指定されたファイルを`src/content/blog/`から読み込み
2. 変更内容を反映
3. 変更をフィーチャーブランチにコミットしてプッシュ
4. **mainブランチにマージしてプッシュ**（GitHub Pagesに反映）

### 記事削除時の動作

1. 指定されたファイルを`src/content/blog/`から削除
2. 変更をフィーチャーブランチにコミットしてプッシュ
3. **mainブランチにマージしてプッシュ**（GitHub Pagesに反映）

### コード変更時の動作

レイアウトやスタイル、コンポーネントなどのコードを変更した場合も同様に：

1. 変更をフィーチャーブランチにコミットしてプッシュ
2. **mainブランチにマージしてプッシュ**（GitHub Pagesに反映）

## 重要：変更のデプロイ

**記事の作成・編集・削除、またはコードの変更を行った場合は、必ずmainブランチにマージしてください。**

これにより、GitHub Actionsが自動的にビルドを実行し、変更がGitHub Pagesに反映されます。

### デプロイの流れ

1. フィーチャーブランチ（`claude/astro-blog-github-pages-*`）で作業
2. 変更をコミット＆プッシュ
3. **mainブランチにチェックアウト**
4. **フィーチャーブランチをmainにマージ**
5. **mainブランチにプッシュ**
6. GitHub Actionsが自動的にビルド＆デプロイ
7. 数分後にhttps://kokushin.github.io/cccms/で変更が反映される

## ファイル構造

```
cccms/
├── src/
│   ├── content/
│   │   └── blog/           # ブログ記事（Markdown）
│   │       ├── welcome.md
│   │       ├── getting-started.md
│   │       └── ...
│   ├── layouts/
│   │   ├── BaseLayout.astro
│   │   └── BlogPostLayout.astro
│   └── pages/
│       ├── index.astro     # トップページ
│       └── blog/
│           ├── index.astro # ブログ一覧
│           └── [...slug].astro  # 個別記事
├── CLAUDE.md               # このファイル
└── astro.config.mjs        # Astro設定
```

## フロントマターの形式

各記事のMarkdownファイルには、以下の形式でメタデータを記述してください：

```yaml
---
title: "記事のタイトル"
date: "YYYY-MM-DD"
author: "著者名"  # オプション
description: "記事の説明文"  # オプション
---

ここから記事の本文をMarkdown形式で記述します。
```

## デプロイ

GitHub Pagesへのデプロイは、mainブランチにプッシュすると自動的に実行されます。

## ローカル開発

ローカルで開発サーバーを起動する場合：

```bash
npm install
npm run dev
```

ビルドする場合：

```bash
npm run build
```

## 注意事項

- 記事のファイル名は、英数字とハイフンのみを使用してください（例: `my-first-post.md`）
- 日付は必ず`YYYY-MM-DD`形式で記述してください
- Markdownの記法については、[CommonMarkの仕様](https://commonmark.org/)を参照してください

## サポート

問題が発生した場合や、新しい機能のリクエストがある場合は、Claude Code on the webで直接質問してください。

---

**Happy Blogging with Claude Code! 🚀**
